這提問了AI還有哪裡可以優化，AI提出了兩點
```cpp
for(int i = 0; i < m-1; ++i){
    for(int j = 0; j < m-1-i; ++j){//多加-i因為外層每做完一次陣列最後都會是正確的位置
        if(a[j] > a[j+1]){
            swap(a[j],a[j+1]) //直接使用內部函式較簡潔
            count++;
        }
    }
}
```